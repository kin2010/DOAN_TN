import { createAsyncThunk, createSlice } from '@reduxjs/toolkit';
const trademark = createSlice({
  initialState: {
    trademark: [],
  },
  reducers: {},
});
const { reducers, actions } = trademark;
export default reducers;
